package Introduction;

public interface Shape {
// any interfce with only one abstract method is called funcitonal interface
	void draw();
	
	// from 1.8 we can havae default and public static methods in inteface
}
